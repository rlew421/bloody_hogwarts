<h1>All Courses at Hogwarts</h1>

<% @courses.each do |course| %>
  <section class= "courses">
    <section id= 'course-<%=course.id%>'>
      <p><%= course.name %>: <%= course.total_enrolled %></p>
    </section>
  </section>
<% end %>
